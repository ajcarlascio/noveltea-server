{
  "name": "noveltea",
  "private": true,
  "description": "NovelTea — self-hosted, offline-first long-form writing app",
  "license": "SEE LICENSE IN LICENSE.md",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "generate": "npm run generate --workspaces --if-present"
  }
}
