{
  "name": "@noveltea/worker",
  "version": "0.1.0",
  "private": true,
  "description": "Runs compile jobs: claims them from Postgres, renders, writes the artifact",
  "license": "SEE LICENSE IN ../LICENSE.md",
  "type": "module",
  "engines": { "node": ">=22.6.0" },
  "scripts": {
    "start": "node src/index.ts",
    "test": "node --test \"test/**/*.test.ts\""
  },
  "dependencies": {
    "@noveltea/compile": "*",
    "pg": "^8.13.1"
  }
}
